#include <stdio.h>

void win(void) {
    char password[] = "5tVd53c";
    int admin;

    if (admin == 21)
        printf("win!\n"); 
}

void work(void) {
    char result[8];
    int addition = 9 + 10;
    gets(result);
    printf("%s\n", result);
}

int main(void) {
    work();

    char buffer[16];
    gets(buffer);
}